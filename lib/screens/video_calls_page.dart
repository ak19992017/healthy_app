import 'package:flutter/material.dart';

class VideoCallsPage extends StatelessWidget {
  const VideoCallsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Video Calls Page");
  }
}
